num = input() # ввод числа как строки
sum = 0 # переменная для суммы
sign = 1 # начинаем с положительного знака

for i in range(len(num)-1, -1, -1): # проходим по числу с конца
    sum += sign * int(num[i]) # прибавляем с учетом знака
    sign *= -1 # меняем знак

print(sum) # выводим результат

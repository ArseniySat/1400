def iter(a, x, e):
    # начальное значение
    y = x
    # задаем функцию для итераций
    f = lambda y: 1/2*(y + a/y)
    # пока не достигнута заданная точность
    while abs(y**2 - a) > e:
        y = f(y)
    return y
